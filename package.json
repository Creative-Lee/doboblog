{
  "name": "doboblog",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "validate-engines": "node scripts/validate-engines.js",
    "preinstall": "node scripts/validate-engines.js"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@>=10",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "semver": "^7.7.2"
  }
}
